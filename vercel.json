{
    "version": 2,
    "builds": [
        {
            "src": "app/**",
            "use": "@vercel/next"
        }
    ],
    "routes": [
        { "handle": "filesystem" },
        { "src": "/(.*)", "dest": "app/$1" }
    ]
}
